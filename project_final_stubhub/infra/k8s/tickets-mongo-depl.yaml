apiVersion: apps/v1
kind: Deployment
metadata:
  name: tickets-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tickets-mongo
  template:
    metadata:
      labels:
        app: tickets-mongo
    spec:
      containers:
        - name: tickets-mongo
          image: mongo # from docker hub
---
apiVersion: v1
kind: Service
metadata:
  name: tickets-mongo-clusterip
spec:
  type: ClusterIP
  selector:
    app: tickets-mongo
  ports:
    - name: tickets-db
      port: 27017
      protocol: TCP
      targetPort: 27017
